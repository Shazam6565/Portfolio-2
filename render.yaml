services:
  - type: web
    name: portfolio-rag-backend
    env: docker
    region: oregon
    plan: free
    branch: main
    rootDir: .
    envVars:
      - key: PORT
        value: 10000
      - key: OPENAI_API_KEY
        sync: false
